General Guidelines

- Never use the em dash "—". Use plain dash "-".
- Never use emojis unless explicitly asked.
- When writing commit messages, NEVER auto-add yourself as a co-author.
- When writing or editing long Markdown files, put each full sentence on its own line. Avoid wrapping lines.
- When building Markdown tables, always pretty print them so columns align when using a monospace font.
